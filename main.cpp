#include "AdvisorBot.h"

//Using a similar structure to the currency_exchange_sim, main creates
//an instance of AdvisorBot and starts the simulation running.
int main(){
    AdvisorBot app{};
    app.init();
}